// Performance caching is handled by the application API client in main.js.
// Do not replace window.fetch globally: doing so duplicates cache state,
// complicates invalidation and can interfere with auth/network requests.
export {}
